@model IList<Quiz>


<div class="container mt-4">
    <div class="row">
        @foreach (var quiz in Model)
        {
            <partial name="QuizCardPartial" model="quiz" />
        }
    </div>
</div>
